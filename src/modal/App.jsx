/**
 * @deprecated Thin re-export — implementation lives under app/ + views/ + components/.
 * Kept so legacy static greps and docs still resolve a root entry.
 */
export { PrModalApp, default } from './app/PrModalApp';
export { mountPrModal } from './app/mountPrModal';
