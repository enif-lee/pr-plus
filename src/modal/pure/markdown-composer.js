/**
 * AUTO-GENERATED from src/modal/lib/markdown-composer.ts
 * SOURCE OF TRUTH: src/modal/lib/markdown-composer.ts
 * Do not edit this file — run: npm run build:pure
 */
(function (global) {
  var module = { exports: {} };
  var exports = module.exports;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var stdin_exports = {};
__export(stdin_exports, {
  SLASH_COMMANDS: () => SLASH_COMMANDS,
  applyInsertion: () => applyInsertion,
  applyMentionInsertion: () => applyMentionInsertion,
  applySlashInsertion: () => applySlashInsertion,
  detectMentionTrigger: () => detectMentionTrigger,
  detectSlashTrigger: () => detectSlashTrigger,
  filterMentions: () => filterMentions,
  filterSlashCommands: () => filterSlashCommands,
  hasMermaidFence: () => hasMermaidFence,
  splitMarkdownSegments: () => splitMarkdownSegments
});
module.exports = __toCommonJS(stdin_exports);
const SLASH_COMMANDS = [
  {
    id: "approve",
    label: "/approve",
    insert: "**LGTM** \u2014 looks good to me.",
    description: "Approve-style praise"
  },
  {
    id: "lgtm",
    label: "/lgtm",
    insert: "LGTM \u{1F44D}",
    description: "Short LGTM"
  },
  {
    id: "nit",
    label: "/nit",
    insert: "**nit:** ",
    description: "Non-blocking nitpick"
  },
  {
    id: "blocking",
    label: "/blocking",
    insert: "**blocking:** ",
    description: "Blocking concern"
  },
  {
    id: "question",
    label: "/question",
    insert: "**question:** ",
    description: "Clarifying question"
  },
  {
    id: "suggestion",
    label: "/suggestion",
    insert: "```suggestion\n\n```",
    description: "GitHub suggestion fence"
  }
];
function splitMarkdownSegments(source) {
  const text = source == null ? "" : String(source);
  if (!text) return [{ type: "md", content: "" }];
  const segments = [];
  const re = /```(?:mermaid|MERMAID)[ \t]*\r?\n([\s\S]*?)```/g;
  let last = 0;
  let m;
  while ((m = re.exec(text)) !== null) {
    if (m.index > last) {
      segments.push({ type: "md", content: text.slice(last, m.index) });
    }
    segments.push({ type: "mermaid", content: m[1].trim() });
    last = m.index + m[0].length;
  }
  if (last < text.length) {
    segments.push({ type: "md", content: text.slice(last) });
  }
  if (segments.length === 0) segments.push({ type: "md", content: text });
  return segments;
}
function hasMermaidFence(source) {
  return splitMarkdownSegments(source).some((s) => s.type === "mermaid");
}
function detectMentionTrigger(text, cursor) {
  const s = String(text || "");
  const c = Math.max(0, Math.min(cursor == null ? s.length : cursor, s.length));
  const before = s.slice(0, c);
  const m = before.match(/(^|[\s([{])@([a-zA-Z0-9-]{0,39})$/);
  if (!m) return null;
  const query = m[2] || "";
  const start = c - query.length - 1;
  return { query, start, end: c };
}
function detectSlashTrigger(text, cursor) {
  const s = String(text || "");
  const c = Math.max(0, Math.min(cursor == null ? s.length : cursor, s.length));
  const before = s.slice(0, c);
  const m = before.match(/(^|[\n\r])(\/)([a-zA-Z0-9_-]{0,32})$/);
  if (!m) return null;
  const query = (m[3] || "").toLowerCase();
  const start = c - query.length - 1;
  return { query, start, end: c };
}
function filterMentions(query, candidates) {
  const q = String(query || "").toLowerCase();
  const list = (Array.isArray(candidates) ? candidates : []).map((c) => typeof c === "string" ? c : c?.login || c?.name || "").filter(Boolean);
  if (!q) return list.slice(0, 8);
  return list.filter((name) => name.toLowerCase().startsWith(q)).slice(0, 8);
}
function filterSlashCommands(query) {
  const q = String(query || "").toLowerCase().replace(/^\//, "");
  if (!q) return SLASH_COMMANDS.slice();
  return SLASH_COMMANDS.filter(
    (cmd) => cmd.id.startsWith(q) || cmd.label.slice(1).startsWith(q)
  );
}
function applyInsertion(text, start, end, insertion, cursorBias = 0) {
  const s = String(text || "");
  const a = Math.max(0, start);
  const b = Math.max(a, end);
  const next = s.slice(0, a) + insertion + s.slice(b);
  const cursor = a + insertion.length + cursorBias;
  return { text: next, cursor };
}
function applyMentionInsertion(text, trigger, username) {
  if (!trigger) return { text: String(text || ""), cursor: String(text || "").length };
  const insert = `@${username} `;
  return applyInsertion(text, trigger.start, trigger.end, insert);
}
function applySlashInsertion(text, trigger, command) {
  if (!trigger || !command) return { text: String(text || ""), cursor: String(text || "").length };
  return applyInsertion(text, trigger.start, trigger.end, command.insert);
}


  var api = module.exports && module.exports.__esModule
    ? (module.exports.default && typeof module.exports.default === 'object'
        ? Object.assign({}, module.exports, module.exports.default)
        : module.exports.default || module.exports)
    : module.exports;
  // Prefer named exports map over default-only
  if (api && typeof api === 'object' && api.__esModule) {
    var flat = {};
    for (var k in api) {
      if (k !== 'default' && k !== '__esModule' && Object.prototype.hasOwnProperty.call(api, k)) {
        flat[k] = api[k];
      }
    }
    if (api.default && typeof api.default === 'object') {
      for (var dk in api.default) {
        if (Object.prototype.hasOwnProperty.call(api.default, dk) && flat[dk] === undefined) {
          flat[dk] = api.default[dk];
        }
      }
    }
    if (Object.keys(flat).length) api = flat;
  }
  global.PRModalMarkdownComposer = api;

})(typeof globalThis !== 'undefined' ? globalThis : typeof window !== 'undefined' ? window : this);
