/**
 * AUTO-GENERATED from src/modal/lib/floating-scrollbar.ts
 * SOURCE OF TRUTH: src/modal/lib/floating-scrollbar.ts
 * Do not edit this file — run: npm run build:pure
 */
(function (global) {
  var module = { exports: {} };
  var exports = module.exports;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var stdin_exports = {};
__export(stdin_exports, {
  FLOATING_SCROLLBAR_IDLE_MS: () => FLOATING_SCROLLBAR_IDLE_MS,
  floatingScrollbarMetrics: () => floatingScrollbarMetrics,
  scrollPosFromThumbDrag: () => scrollPosFromThumbDrag,
  scrollTopFromThumbDrag: () => scrollTopFromThumbDrag
});
module.exports = __toCommonJS(stdin_exports);
const MIN_THUMB = 28;
const FLOATING_SCROLLBAR_IDLE_MS = 1e3;
function floatingScrollbarMetrics(scrollPos, clientSize, scrollSize) {
  const st = Number(scrollPos) || 0;
  const ch = Number(clientSize) || 0;
  const sh = Number(scrollSize) || 0;
  if (!(ch > 0) || !(sh > ch + 1)) {
    const empty = {
      needed: false,
      thumbSize: 0,
      thumbOffset: 0,
      trackSize: Math.max(0, ch),
      thumbHeight: 0,
      thumbTop: 0,
      trackHeight: Math.max(0, ch)
    };
    return empty;
  }
  const thumbSize = Math.max(MIN_THUMB, Math.round(ch / sh * ch));
  const maxScroll = sh - ch;
  const maxOffset = ch - thumbSize;
  const thumbOffset = maxScroll <= 0 ? 0 : Math.round(Math.min(st, maxScroll) / maxScroll * maxOffset);
  const clamped = Math.max(0, Math.min(maxOffset, thumbOffset));
  return {
    needed: true,
    thumbSize,
    thumbOffset: clamped,
    trackSize: ch,
    thumbHeight: thumbSize,
    thumbTop: clamped,
    trackHeight: ch
  };
}
function scrollTopFromThumbDrag(pointerInTrack, thumbSize, clientSize, scrollSize) {
  const y = Number(pointerInTrack);
  const th = Number(thumbSize);
  const ch = Number(clientSize);
  const sh = Number(scrollSize);
  if (!Number.isFinite(y) || !(ch > 0) || !(sh > ch)) return 0;
  const maxTop = Math.max(1, ch - Math.max(MIN_THUMB, th || MIN_THUMB));
  const maxScroll = sh - ch;
  const top = Math.max(0, Math.min(maxTop, y - (th || MIN_THUMB) / 2));
  return Math.round(top / maxTop * maxScroll);
}
const scrollPosFromThumbDrag = scrollTopFromThumbDrag;


  var api = module.exports && module.exports.__esModule
    ? (module.exports.default && typeof module.exports.default === 'object'
        ? Object.assign({}, module.exports, module.exports.default)
        : module.exports.default || module.exports)
    : module.exports;
  // Prefer named exports map over default-only
  if (api && typeof api === 'object' && api.__esModule) {
    var flat = {};
    for (var k in api) {
      if (k !== 'default' && k !== '__esModule' && Object.prototype.hasOwnProperty.call(api, k)) {
        flat[k] = api[k];
      }
    }
    if (api.default && typeof api.default === 'object') {
      for (var dk in api.default) {
        if (Object.prototype.hasOwnProperty.call(api.default, dk) && flat[dk] === undefined) {
          flat[dk] = api.default[dk];
        }
      }
    }
    if (Object.keys(flat).length) api = flat;
  }
  global.PRModalFloatingScrollbar = api;

})(typeof globalThis !== 'undefined' ? globalThis : typeof window !== 'undefined' ? window : this);
